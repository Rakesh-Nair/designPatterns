Objects use other objects fields and methods through inheritance and composition.

allows to treat both single and composite objects similarly

Composite pattern is a mechanism for treating individual objects and compositions of objects in a uniform manner.

Java supports container iteration with iterable interface

A single object can masquerade as a collection by returning single element collection containing only this.